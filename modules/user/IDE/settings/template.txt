{
	// Place your GLOBAL snippets here. Each snippet is defined under a snippet name and has a scope, prefix, body and 
	// description. Add comma separated ids of the languages where the snippet is applicable in the scope field. If scope 
	// is left empty or omitted, the snippet gets applied to all languages. The prefix is what is 
	// used to trigger the snippet and the body will be expanded and inserted. Possible variables are: 
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. 
	// Placeholders with the same ids are connected.
	// Example:
	"flake parts": {
		"scope": "nix",
		"prefix": "fp",
		"body": [
			"{ inputs, ... }:",
			"",
			"{",
			"    flake.modules.${1|darwin,homeManager|}.$2 = { config, pkgs, ... }: let",
			"        ",
			"    in {",
			"        ",
			"    };",
			"}"
		],
		"description": "flake parts template"
	},
	"nix platform": {
		"scope": "nix",
		"prefix": "np",
		"body": [
			"pkgs.stdenv.hostPlatform.system"
		],
		"description": "get current platform in nix"
	},
	"home symlink": {
		"scope": "nix",
		"prefix": "symlink",
		"body": [
			"config.lib.file.mkOutOfStoreSymlink \"${config.home.homeDirectory}/sysconfig/modules/user/$1\";"
		],
		"description": "home manager symlink"
	},
	"home file": {
		"scope": "nix",
		"prefix": "hf",
		"body": [
			"home.file.\"$1\".source = $2;"
		],
		"description": "redirect file location using home manager"
	}
	//
	// You can also restrict snippets to specific files using include/exclude patterns:
	// "Test snippet": {
	// 	"scope": "javascript,typescript",
	// 	"prefix": "test",
	// 	"body": "test('$1', () => {\n\t$0\n});",
	// 	"include": ["**/*.test.ts", "*.spec.ts"],
	// 	"exclude": ["**/temp/*.ts"],
	// 	"description": "Insert test block"
	// }
}